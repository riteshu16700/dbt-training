version: 2

models:
  - name: stage_orders
    description: one unique order per row
    columns:
      - name: orderid
        description: primary key for stage orders
      - name: shipmode
        description: '{{docs("shipmode")}}''